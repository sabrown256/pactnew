#!/bin/csh -f
#
# NO-OP - dummy script
#       - it takes any number of arguments
#       - and returns an exit status of 0
#
# Source Version: 3.0
# Software Release #: LLNL-CODE-422942
#
# include "cpyright.h"
#

unalias *

if (($#argv > 0) && ("$1" == "-exit")) then
   set st = $argv[2]
else
   set st = 0
endif

exit($st)

