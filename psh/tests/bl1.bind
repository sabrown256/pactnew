#
# BL1.BIND - basic blang test
#
# Source Version: 3.0
# Software Release #: LLNL-CODE-422942
#
# include "cpyright.h"
#

# treat a struct as a character array - unless specific definition exists
struct str
enum etyp 0

# C	Fortran	Scheme	Python

fav1	fortran(fav1f)	scheme(fav1s)	python(fav1p)
fav2	fortran(fav2f)	scheme(fav2s)	python(fav2p)
fav3	fortran(fav3f)	scheme(fav3s)	python(fav3p)
fav4	fortran(fav4f)	scheme(fav4s)	python(fav4p)
favp1	fortran(favp1f)	scheme(favp1s)	python(favp1p)
favp2	fortran(favp2f)	scheme(favp2s)	python(favp2p)
favp3	fortran(favp3f)	scheme(favp3s)	python(favp3p)
favp4	fortran(favp4f)	scheme(favp4s)	python(favp4p)

fav5	fortran()	scheme()	python()
fav6
